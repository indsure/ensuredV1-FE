{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://policy.sach.ai/schemas/ClauseReference.schema.json",
  "title": "ClauseReference",
  "description": "Mandatory reference to a specific location in the policy document.",
  "type": "object",
  "required": [
    "clause_id",
    "page_number",
    "text_snippet",
    "confidence"
  ],
  "properties": {
    "clause_id": {
      "type": "string",
      "description": "The specific clause number or section header (e.g., '4.1', 'Section B')."
    },
    "page_number": {
      "type": "integer",
      "minimum": 1,
      "description": "The page number in the original PDF."
    },
    "text_snippet": {
      "type": "string",
      "minLength": 5,
      "description": "Verbatim text quoted from the policy."
    },
    "confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Confidence score of the extraction."
    }
  },
  "additionalProperties": false,
  "_compatibility": {
    "version": "1.0.0",
    "rules": "Strict requirement for all findings."
  }
}