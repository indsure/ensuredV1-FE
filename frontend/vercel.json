{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/a/:slug", "destination": "/api/advisor-page?slug=:slug" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
